version: "3.9"

services:
  jackbox:
    build: .
    image: jackbox
    ports:
      - "22:22"
    volumes:
      - "var:/var"
      - "etc:/etc"
      - "home:/home"

volumes:
  var:
  etc:
  home:
