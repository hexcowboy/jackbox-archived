version: "3.9"

services:
  jackbox:
    build: .
    image: jackbox
    ports:
      - "22:22"
    volumes:
      - "etc:/etc"
      - "home:/home"
      - "opt:opt"
      - "var:/var"

volumes:
  etc:
  home:
  opt:
  var:
